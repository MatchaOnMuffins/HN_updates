{
  "name": "hn-updates-mcp",
  "version": "1.0.0",
  "description": "Cloudflare Worker MCP server that fetches the current top Hacker News stories.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.12.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260509.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.90.0"
  }
}
